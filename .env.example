# Required
CRON_SECRET=replace_me_with_a_random_string

GITHUB_TOKEN=replace_me_with_a_github_token_with_contents_write
GITHUB_OWNER=replace_me (user or org)
GITHUB_REPO=replace_me
# Optional: if empty, GitHub uses the default branch
GITHUB_BRANCH=
# Optional: defaults to "status.txt"
GITHUB_FILE_PATH=status.txt

# Optional: set to "random2or4" to run 2 or 4 times/day (deterministic by date)
MODE=
